# Test environment variables
amocrm_client_id=test_client_id
amocrm_client_secret=test_client_secret
amocrm_redirect_uri=http://localhost:8000/api/auth/amo/callback
amocrm_refresh_token=test_refresh_token
amocrm_domain=test.amocrm.ru

# Database
db_url=postgresql://test:test@localhost:5432/test_db

# Frontend URL
frontend_url=http://localhost:3000

# Security
secret_key=test_secret_key_for_testing_only
jwt_secret=test_jwt_secret_for_testing_only

# Email settings
smtp_server=smtp.test.com
smtp_port=587
smtp_user=test@test.com
smtp_password=test_password

# WhatsApp settings
whatsapp_api_url=https://api.test.whatsapp.com
whatsapp_api_key=test_whatsapp_key

# Logging
log_level=DEBUG

# Security
debug=true
allowed_hosts=["localhost", "127.0.0.1"]
